@using CarShop.Core.Models
@model UserRolesViewModel
@{
	ViewData["Title"] = "User Roles";
	var items = (List<SelectListItem>)ViewBag.RoleItems;
}

<link rel="stylesheet" href="~/css/style/edit-user-style.css">

<div class="main">
	<form method="post">
		@*<input type="hidden" name="UserId" value="@Model.UserId" />
			<div class="form-floating mb-3">
			<h4>@Html.DisplayFor(m => m.Name)</h4>
			</div>
			<div class="form-floating mb-3">
			<div class="form-group">
			<label>User Roles</label>
			<select style="display: none;" asp-for="RoleNames" asp-items="@items" multiple="" class="form-control" id="roles">
			</select>
			<div class="roles-checks">
			@foreach (var item in @items)
			{
			<input name="@item.Value" type="checkbox" class="role" value="@item.Value">
			<label for="@item.Value">@item.Text</label>
			<br />
			}
			</div>
			</div>
			</div>

			<div class="pt-1 mb-4">
			<button id="login-submit" type="submit" class="btn btn-lg btn-block">Save</button>
			</div>*@

		<h2>Roles of @Html.DisplayFor(m => m.Name)</h2>

		<select id="roles-select" asp-for="RoleNames" asp-items="items"></select>

		<div class="roles">
			@foreach (var role in items)
			{
				<div class="role">
					<input type="checkbox" name="@role.Value.ToLower()" value="@role.Value">
					<label for="@role.Value.ToLower()">@role.Text</label>
				</div>
			}
		</div>

		<div class="actions">
			<button type="submit" class="save">Save</button>
		</div>
	</form>
</div>

<script src="~/js/roles.js"></script>