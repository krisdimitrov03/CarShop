@using CarShop.Core.Models
@model CarCardViewModel
@{
	ViewData["Title"] = "Delete Car";
}

<link rel="stylesheet" href="~/css/style/delete-style.css">

<form method="post">
	@*<h1>Are you sure that you want to remove this car from the database?</h1>
		<div class="card" style="width: 18rem;">
		<img class="card-img-top" src="@Model.ImageUrl" style="height: 150px; object-fit: cover;" alt="Card image cap">
		<div class="card-body">
		<h5>@Model.Brand @Model.Model</h5>
		</div>
		</div>

		<button asp-area="Emp" asp-controller="Cars" asp-action="Delete" type="submit" class="btn btn-danger">Yes</button>
		<a asp-area="" asp-controller="Cars" asp-action="Details" asp-route-id="@Model.Id" class="btn btn-primary">Cancel</a>*@

	<div class="main">
		<div class="question">
			<h3>Are you sure you want to delete this vehicle from the database?</h3>
		</div>

		<div class="vehicle-section" style="background: linear-gradient(rgba(0, 0, 0, 0.5),
											rgba(0, 0, 0, 0.5)),
											url('@Model.ImageUrl');
											background-position: center;
											background-size: cover;">
			<div class="id">
				<h3>@Model.Model</h3>
				<h4>@Model.Brand</h4>
			</div>
		</div>

		<div class="actions">
			<button type="submit" class="delete">Delete</button>
			<a asp-area="" asp-controller="Cars" asp-action="Details" asp-route-id="@Model.Id">Cancel</a>
		</div>
	</div>
</form>
